# flutter-widgets-everything-is-a-widget
This Flutter example demonstrates the core concept that "everything is a widget." It showcases how an entire application, its layout, text, buttons, and even icons are all composed of various widgets nested within each other. The app features a simple counter that increments when a button is pressed, illustrating both stateless and stateful widgets
